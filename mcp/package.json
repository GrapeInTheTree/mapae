{
  "name": "mapae-mcp",
  "version": "0.3.0",
  "description": "MCP server for Mapae - scoped, identity-bound spending authority on GIWA. The agent gets use of an authority, never the wallet.",
  "license": "MIT",
  "type": "module",
  "bin": {"mapae-mcp": "dist/index.js"},
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "start": "node dist/index.js"
  },
  "repository": {"type": "git", "url": "https://github.com/GrapeInTheTree/mapae"},
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "viem": "^2.55.10",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "~5.9.0"
  }
}
