# Compiler files
cache/
out/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# forge init ships `docs/` here for mdbook build output; ours is the hand-written
# submission surface (SPEC, DEMO, GAPS, the README's brand marks) and must ship.
# Only the heavyweight design explorations stay local.
docs/design/

# Dotenv file
.env

# secrets
.env
.env.*
!.env.example

# build
out/
cache/
broadcast/*/dry-run/
node_modules/

# local agent config
.claude/
