# The index accelerates the explorer; the client falls back to reading GIWA directly whenever it
# does not answer within its deadline, so this line can be removed at any time without breaking
# the product - see src/lib/indexer.ts.
VITE_INDEXER_URL=https://index.mapae.org
